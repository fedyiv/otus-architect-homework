{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "fedyiv-otus-user-service-hw6-chart.serviceAccountName" . }}
  labels:
    {{- include "fedyiv-otus-user-service-hw6-chart.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
