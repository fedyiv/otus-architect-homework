apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fedyiv-otus-order-service-hw6-chart.fullname" . }}-test-connection"
  labels:
    {{- include "fedyiv-otus-order-service-hw6-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fedyiv-otus-order-service-hw6-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
